% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe_vizr.R
\name{pipe_vizr}
\alias{pipe_vizr}
\title{Visualize a dplyr-style pipeline with icons}
\usage{
pipe_vizr(
  ...,
  direction = "LR",
  theme = "default",
  show_expressions = TRUE,
  fixed_width = TRUE,
  return_graph = FALSE
)
}
\arguments{
\item{...}{A single pipeline expression}

\item{direction}{Graph layout direction: "LR" (left-right), "TB" (top-bottom), 
"RL" (right-left), "BT" (bottom-top)}

\item{theme}{Visual theme: "default", "minimal", or "colorful"}

\item{show_expressions}{Show the RHS of assignments in mutate/summarize nodes}

\item{fixed_width}{Make all nodes the same width (TRUE by default)}

\item{return_graph}{If TRUE, return the graph object instead of rendering}
}
\description{
Visualize a dplyr-style pipeline with icons
}
\examples{
\dontrun{
library(dplyr)

# Basic usage with fixed width
pipe_vizr(
  mtcars \%>\%
    filter(cyl > 4, mpg > 20) \%>\%
    arrange(desc(mpg)) \%>\%
    select(mpg, cyl, hp) \%>\%
    mutate(
      efficiency = mpg / hp,
      power_ratio = hp / cyl
    ) \%>\%
    summarize(
      avg_eff = mean(efficiency),
      max_eff = max(efficiency)
    )
)

# Without fixed width (variable node widths)
pipe_vizr(
  mtcars \%>\%
    filter(cyl > 4) \%>\%
    select(mpg),
  fixed_width = FALSE
)
}

}
